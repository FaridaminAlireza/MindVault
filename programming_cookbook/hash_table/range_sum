arr = [1, 2, 3, 4, 5]
prefix = [0] * len(arr)
prefix[0] = arr[0]

for i in range(1, len(arr)):
    prefix[i] = prefix[i-1] + arr[i]

def range_sum(L, R):
    return prefix[R] - (prefix[L-1] if L > 0 else 0)

print(range_sum(1, 3))  # Sum from index 1 to 3 -> 2+3+4 = 9